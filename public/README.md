# Public Asset Placeholders

Current temporary production placeholders:

- `favicon.svg` is a minimal RF monogram favicon.
- `og-image.svg` is a temporary 1200x630 Open Graph/Twitter preview image.

These are wired in `app/layout.tsx` and can be replaced later with final branded PNG/JPG/ICO assets.
